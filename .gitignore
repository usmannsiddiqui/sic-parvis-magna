# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
.astro/

# Environment
.env
.env.*
!.env.example

# Test artifacts
test-results/
playwright-report/
playwright/.cache/
coverage/

# Editor / OS
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json

# Design tooling (impeccable) — installed locally via `npx impeccable install`, not vendored
.claude/skills/
.claude/settings.local.json
.impeccable/

# Design wireframe bundle — contains watermarked third-party reference images
# (someone else's copyright, per design.md). Local-only; never commit to this public repo.
sic-parvis-magna-home/
